import React from 'react';

const RegisterPage = () => {
  // This component exists for backward compatibility
  // The actual auth logic is handled in LoginPage.tsx
  // When /register is accessed, LoginPage detects it via pathname
  return null;
};

export default RegisterPage;
